import { PluginOption } from 'vite';
export * from '@webspatial/shared';

interface WebSpatialOptions {
    mode?: 'avp';
    outputDir?: string;
}
declare function export_default(options?: WebSpatialOptions): PluginOption[];

export { export_default as default };
